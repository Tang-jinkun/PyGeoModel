export interface AirCorridorPointInput { lon: number; lat: number; altitude_m: number; altitude_mode: "agl" | "amsl" }
export interface AirCorridorAircraftInput { cruise_speed_kph: number; min_agl_m: number; max_agl_m: number; max_climb_rate_mps: number; max_descent_rate_mps: number }
export interface AirDefenseThreatInput { id: string; name: string | null; lon: number; lat: number; min_range_m: number; max_range_m: number; min_altitude_m: number; max_altitude_m: number; threat_level: number; kill_zone_radius_m: number | null; warning_zone_radius_m: number | null }
export interface AirCorridorPlanningInput { corridor_width_m: number; horizontal_resolution_m: number; allow_altitude_change: boolean; threat_weight: number; distance_weight: number; altitude_change_weight: number; terrain_clearance_weight: number; output_simplify_tolerance_m: number | null }
export interface AirCorridorRequest { dem_id: string; start: AirCorridorPointInput; end: AirCorridorPointInput; aircraft: AirCorridorAircraftInput; altitude_layers_m: number[]; threats: AirDefenseThreatInput[]; planning: AirCorridorPlanningInput }
export interface AirCorridorMetrics { route_found: boolean; failure_reason: string | null; risk_score: number | null; max_segment_risk: number | null; mean_segment_risk: number | null; corridor_length_m: number; estimated_time_seconds: number | null; min_terrain_clearance_m: number | null; mean_terrain_clearance_m: number | null; altitude_change_count: number; min_altitude_m: number | null; max_altitude_m: number | null; threat_intersection_count: number; nearest_threat_distance_m: number | null }
